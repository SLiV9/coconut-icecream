extern void printInt(int n);

int m = 2;
float[m, 3] a;
int i = foo(a, 2);
float[m, 3] b;

extern float[w, k] e;

int baz(float[k1, k2] \
	a, int i)
{
	int[k1, k2] u;
	m = m + (int) a[k1-i, k2-i];
	return (int) e[k1, m];
}

int foo(int y)
{
	int z = x * y;
	int bar(int x, int y)
	{
		return x + y;
	}
	foo(y);
	x = foo(z) + bar(z, y);
	return x + y;
}

void main()
{
	foo(b, 3);
	printInt(m);
}
