extern void printInt(int n);

int m = 2;
float[m, 3] a;
int i = foo(a, 2);
float[m, 3] b;

extern float[w, k] e;

int foo(float[k1, k2] \
	a, int i)
{
	int[k1, k2] u;
	m = m + (int) a[k1-i, k2-i];
	return (int) e[k1, m];
}

void main()
{
	foo(b, 3);
	printInt(m);
}
