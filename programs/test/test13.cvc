extern void printInt(int val);
extern void printSpaces(int num);
extern void printNewlines(int num);

extern int scanInt();

export int main()
{
	int x = 5;
	int i = 2;
	bool e = true;
	
	bool isEven(int x)
	{
		bool r = true;
		
		if (x > 0)
		{
			r = isOdd(x - 1);
		}
		
		return r;
	}
	
	bool isOdd(int x)
	{
		bool r = false;
		
		if (x > 0)
		{
			r = isEven(x - 1);
		}
		
		return r;
	}
	
	for (int i = 0, 10, i)
	{
		e = isTrue(e && isEven(x + i)); 
	}
	
	x = i;
	
	return 0;
}

bool isTrue(bool a)
{
	return (a);
}
