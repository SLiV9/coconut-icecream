extern void printFloat(float f);

float[3] y = [30.0, -1.23, 5.0];

export void main()
{
	int n = 5;
	float[n] x = [3.1415, -1.0, 0.0, 1.337, 303.03];
	float[2, n] y = [[4.24, 2.], [3.1, 3.1]];
	
	float sum(float[k] a)
	{
		float sum = 0.0;
		
		for (int i = 0, k)
		{
			sum = sum + a[k];
		}
		
		return sum;
	}
	
	float average(float[k] a)
	{
		float av;
		
		if (k > 0)
		{
			av = sum(a) / (float) k;
		}
		else
		{
			av = 0.0;
		}
		
		return av;
	}
	
	float getIth(float[k] a, int i)
	{
		float r = 0.0;
		if (i >= 0 && i < k)
		{
			r = a[i];
		}
		return r;
	}
	
	printFloat(average(x));
	printFloat(sum([1.23, -4.55, 0.22]));
	printFloat(getIth(x, 2));
}

