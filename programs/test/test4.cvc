float y1 = -1.23;
float[3] y = [30.0, y1, 5.0];
export float[5] z;
extern float[m] u;

export void main()
{
	int n = 5;
	int m = 2;
	float[n] x = [3.1415, -1.0, 0.0, 1.337, 303.03];
	float[2, m] y = [[3.1415, -1.0], [0.0, 1.337]];
	
	float getIth(float[k] a, int i)
	{
		float r = 0.0;
		if (i >= 0 && i < k)
		{
			r = a[i];
		}
		return r;
	}
	
	float remake(float[k] a)
	{
		float[k] b;
		for (int i = 0, k)
		{
			b[k] = a[k];
		}
		return 0.0;
	}
	
	getIth(y, -1);
}

int foo(float[k] a)
{
	for (int i = 0, k)
	{
		a[i] = 2.0 * (float) k;
		i = i + 1;
		k = k - 1;
	}
	return k;
}
